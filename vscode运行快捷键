按 Ctrl + Shift + P 打开命令面板
输入 keybindings 并选择 Preferences: Open Keyboard Shortcuts (JSON)
（会打开一个 keybindings.json 文件）
把下面这段代码复制粘贴到文件的最末尾（在最后一个 ] 之前加上逗号）：

JSON{
        "key": "f5",
        "command": "python.execInTerminal",
        "when": "editorTextFocus && editorLangId == 'python'"
    }

保存文件（Ctrl+S）
